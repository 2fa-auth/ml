sdsdasdasdsadasdsdasasdsdskajknjnjjnsdjknajkna`kjnasdjkanjknsdfjnasjkdjknasdjnjasnkndsn`jnajkdn



                \elf.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
ort cv2ASD import cv2ASD import cv2 import cv2 import cv2 import cv2
ort cv2ASD import cv2ASD import cv2 import cv2 import cv2 import cv2
ort cv2ASD import cv2ASD import cv2 import cv2 import cv2 import cv2
ort cv2ASD import cv2ASD import cv2 import cv2 import cv2 import cv2
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
             asdasdasdlasdmklasdjkasdjkna.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.window_scale = min(1.0, self.window_scale + 0.1)
                isdasd`asdasd`sd:self.window_scale = min(1.0, self.window_scale + 0.1)
                ssd:sdasdasdasdasdasssdasdasds
absdsdelf.edisplay_heighasdasdasasdf.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)asd
            elif key == ord('-') or key == ord('_'):
                self.window_scale = max(0.3, self.window_scale - 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord(' '):
                paused = not paused
                print(f"{'⏸️ Пауза' if paused else '▶️ Продолжить'}")
        
                self.mask = None
                self.cap.set(cv2.CAP_PROP_POS_FRAMES, 0)
                self.frame_count = 0
            elif key == ord('+') or key == ord('='):
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord('-') or key == ord('_'):
                self.window_scale = max(0.3, self.window_scale - 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord(' '):
                paused = not paused
                print(f"{'⏸️ Пауза' if paused else '▶️ Продолжить'}")
        
                self.mask = None
                self.cap.set(cv2.CAP_PROP_POS_FRAMES, 0)
                self.frame_count = 0
            elif key == ord('+') or key == ord('='):
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord('-') or key == ord('_'):
                self.window_scale = max(0.3, self.window_scale - 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord(' '):
                paused = not paused
                print(f"{'⏸️ Пауза' if paused else '▶️ Продолжить'}")
        
                self.mask = None
                self.cap.set(cv2.CAP_PROP_POS_FRAMES, 0)
                self.frame_count = 0
            elif key == ord('+') or key == ord('='):
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizedasdasdindow(self.window_name, self.display_width, self.display_height)
            elif key == ord('-') or key == ord('_'):
                self.window_scale = max(0.3, self.window_scale - 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord(' '):
                paused = not paused
                print(f"{'⏸️ Пауза' if paused else '▶️ Продолжить'}")
        
                self.mask = None
                self.cap.set(cv2.CAP_PROP_POS_FRAMES, 0)
                self.frame_count = 0
            elif key == ord('+') or key == ord('='):
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord('-') or key == ord('_'):
                self.window_scale = max(0.3, self.window_scale - 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord(' '):
                paused = not paused
                print(f"{'⏸️ Пауза' if paused else '▶️ Продолжить'}")

         

        
                self.mask = None
                self.cap.set(cv2.CAP_PROP_POS_FRAMES, 0)
                self.frame_count = 0 elif key == ord('+') or key == ord('='): self.window_scale = min(1.0, self.window_scale + 0.1) self.display_width = int(self.width * self.window_scale) self.display_height = int(self.height * self.window_scale) cv2.resizeWindow(self.window_name, self.display_width, self.display_height) elif key == ord('-') or key == ord('_'):
                self.window_scale = max(0.3, self.window_scale - 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord(' '):
                paused = not paused
                print(f"{'⏸️ Пауза' if paused else '▶️ Продолжить'}")
        
                self.mask = None
                self.cap.set(cv2.CAP_PROP_POS_FRAMES, 0)
                self.frame_count = 0
            elif key == ord('+') or key == ord('='):
                self.window_scale = min(1.0, self.window_scale + 0.1)
                self.displassasdasdasdasdasdhello heightasdasdaRasdasdadindow_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord('-') or key == ord('_'):
                self.window_scale = max(0.3, self.window_scale - 0.1)
                self.display_width = int(self.width * self.window_scale)
                self.display_height = int(self.height * self.window_scale)
                cv2.resizeWindow(self.window_name, self.display_width, self.display_height)
            elif key == ord(' '):
                paused = not paused
                print(f"{'⏸️ Пауза' if paused else '▶️ Продолжить'}")
       rrddrsdasdasd self.mask = None self.mask = None
                self.mask = None
                self.mask = None
                self.mask = None
                self.mask = None
                self.mask = None
                self.mask = None
                self.mask = None
                self.mask = None
                self.mask = None
        self.cleanup()









U

iii
ooo
o



       ept FileNotFoundError:
        print("❌ Файл drive_car.mp4 не найден!")
    except Exception as e:
        print(f"❌ Ошибка: {e}")